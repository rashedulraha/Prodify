import React from "react";

const Logout = () => {
  return (
    <div>
      <div className=" hover:text-blue-600 font-bold cursor-pointer">
        Logout
      </div>
    </div>
  );
};

export default Logout;
